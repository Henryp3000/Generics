public class Notes {

    /*
    Generics offer the ability to have one class instead of having a Double Printer class, String class, and Integer class.\


    Generics don't work with primitive types int long but if you use the wrapperclass like integer it will work the same way.

    offer the abality to create classes that can create different types.

    Generics terms
    <E> :formal parameter here and reprsetents Generic Type
    <Integer> :Integer is an parameter represents Parameterized Type
    <T extends Comparable>  T is a bound type parameter and represents a Generic Type.
    <?> unbound wildcart type parameter
    <? extends T> :unbound wildcart type parameter
    <?Super T >
    T extends Comparable <T> :

    Generic naming conventions
    T : It represents type.
    E : It represents element.
    K – It represents keys.
    V – It represents values.
    N – It represents numbers.



     */
}
